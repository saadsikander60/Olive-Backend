# Olive-Backend

Olive Skin Pharma e-commerce API (Node.js, Express, MongoDB, Socket.IO).

## Setup

```bash
npm install
cp .env.example .env
# Edit .env — MONGODB_URI must target database `olive`
npm run seed:admin
npm run dev
```

API base: `http://localhost:5000/api/v1`
